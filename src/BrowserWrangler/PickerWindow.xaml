<Window
    x:Class="BrowserWrangler.PickerWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border
        Background="{ThemeResource LayerFillColorDefaultBrush}"
        BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
        BorderThickness="1"
        CornerRadius="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid
                x:Name="TitleBarHost"
                Padding="12,4,6,4"
                BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
                BorderThickness="0,0,0,1"
                ColumnSpacing="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Grid Grid.Column="0" x:Name="TitleBarDragRegion">
                    <TextBlock
                        x:Name="TitleBarText"
                        FontSize="13"
                        FontWeight="SemiBold"
                        Text="Browser Wrangler" />
                </Grid>
                <Button
                    x:Name="CloseButton"
                    Grid.Column="1"
                    Padding="6"
                    Background="Transparent"
                    BorderBrush="Transparent"
                    Click="Close_Click"
                    ToolTipService.ToolTip="Close">
                    <FontIcon FontSize="12" Glyph="&#xE711;" />
                </Button>
            </Grid>

            <Grid Grid.Row="1" Padding="12,8,8,8" ColumnSpacing="8" RowSpacing="2" BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}" BorderThickness="0,0,0,1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock
                    x:Name="UrlText"
                    VerticalAlignment="Center"
                    FontSize="12"
                    Opacity="0.7"
                    TextWrapping="Wrap"
                    MaxLines="2"
                    TextTrimming="CharacterEllipsis" />
                <Button
                    x:Name="CopyButton"
                    Grid.Column="1"
                    VerticalAlignment="Top"
                    Padding="6"
                    Background="Transparent"
                    BorderBrush="Transparent"
                    Click="Copy_Click"
                    ToolTipService.ToolTip="Copy URL and close">
                    <FontIcon FontSize="13" Glyph="&#xE8C8;" />
                </Button>
            </Grid>

            <ListView
                x:Name="ProfileList"
                Grid.Row="2"
                Padding="6"
                IsItemClickEnabled="True"
                ItemClick="ProfileList_ItemClick"
                SelectionMode="Single"
                SingleSelectionFollowsFocus="True"
                ScrollViewer.VerticalScrollBarVisibility="Auto">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="MinHeight" Value="0" />
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    </Style>
                </ListView.ItemContainerStyle>
            </ListView>
        </Grid>
    </Border>
</Window>
