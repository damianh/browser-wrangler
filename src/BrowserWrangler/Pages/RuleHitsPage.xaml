<Page
    x:Class="BrowserWrangler.Pages.RuleHitsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Padding="24" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="Rule hits" />
        <TextBlock
            Grid.Row="1"
            FontSize="12"
            Opacity="0.7"
            TextWrapping="Wrap"
            Text="Shows recent routing decisions when logging is enabled in Settings." />

        <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="8">
            <Button Click="Refresh_Click" Content="Refresh" />
            <Button Click="Clear_Click" Content="Clear log" />
        </StackPanel>

        <Grid Grid.Row="3">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="EntriesPanel" Spacing="8" />
            </ScrollViewer>

            <TextBlock
                x:Name="EmptyStateText"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Text="No rule-hit entries yet."
                Opacity="0.7"
                Visibility="Collapsed" />
        </Grid>
    </Grid>
</Page>
